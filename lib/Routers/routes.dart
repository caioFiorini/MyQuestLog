import 'package:flutter/material.dart';
import 'package:go_router/go_router.dart';
import 'package:myquestlog/homePage.dart';

final routes = GoRouter(
  routes: [
    // GoRoute(path: '/',
    // builder: (
    //   //(context, state) => HomePage(),
    // ),
    //pageBuilder: MaterialPage(child: HomePage()))
  ],
);
